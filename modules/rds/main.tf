# See root main.tf for the full RDS resource definitions.
# This file is the entry point when using modules/rds as a module source.
# Copy the contents of root main.tf here when refactoring to module layout.

# Placeholder — the production code lives in root main.tf (flat layout)
# and is fully functional. To use as a child module, move root main.tf
# content here and update examples/rds/main.tf to source = "../../modules/rds".
